suite: Samples/IT Tools
templates:
  - templates/*.yaml
values:
  - "../docs/snippets/samples/it-tools.yaml"
tests:
  - it: manifest should match snapshot
    asserts:
      - matchSnapshot: {}
