suite: Ensure no changes in manifests compared to saved snapshot (no unintentional changes)
templates:
  - templates/*.yaml
values:
  - "../sample-values/values.linter.yaml"
  - "../sample-values/configmaps.yaml"
  - "../sample-values/secrets.yaml"
tests:
  - it: manifest should match snapshot
    asserts:
      - matchSnapshot: {}
